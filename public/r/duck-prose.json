{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "duck-prose",
  "title": "Duck Prose",
  "description": "The long-form surface: an article, a post, a changelog entry. Styles markup it has never seen — bare h2, p, blockquote, pre, table — from the theme tokens, at a 68ch measure. Every rule is wrapped in :where(), so a utility on any element inside still wins.",
  "registryDependencies": [
    "@duck/theme"
  ],
  "files": [
    {
      "path": "registry/duck/ui/duck-prose.tsx",
      "content": "import * as React from \"react\";\n\nimport { cn } from \"@/lib/utils\";\n\n/**\n * DuckProse — the long-form surface: an article, a changelog entry, whatever\n * a CMS or an MDX file hands over as bare tags.\n *\n * Every duck component before this one styled markup it could see. Prose\n * styles markup it cannot: the content arrives as <h2>, <p>, <blockquote>,\n * <table> with no class anywhere, so the rules have to be descendant selectors\n * on one wrapper. That is the whole reason @tailwindcss/typography exists —\n * and the reason it is not used here is that it ships its own type scale,\n * its own greys and its own idea of a blockquote, none of which are duck's or\n * a theme's. This reads the tokens instead: --font-display for headings,\n * --font-mono for code and table headers, --primary for the rules and marks,\n * --radius for anything boxed.\n *\n * Measure is the one thing a consumer must not have to think about. 68ch is\n * the default because that is where a line stops being pleasant to read;\n * `measure=\"wide\"` is for a docs page that already sits in a narrow column,\n * and full turns it off for a table-heavy page.\n *\n * Selectors use :where() throughout, so every one of them is zero specificity\n * and a utility on the element itself always wins. `class=\"text-2xl\"` on a\n * heading inside prose has to work, or the component is a cage.\n */\n\nconst measures = {\n  /** 68ch. Long-form default. */\n  default: \"[--prose-measure:68ch]\",\n  /** 76ch, for a page that is already narrow. */\n  wide: \"[--prose-measure:76ch]\",\n  /** No limit — the container decides. */\n  full: \"[--prose-measure:none]\",\n} as const;\n\nexport interface DuckProseProps extends React.ComponentProps<\"div\"> {\n  measure?: keyof typeof measures;\n  /**\n   * Render as this element instead of a div. Pass \"article\" for a post — the\n   * wrapper is the article, not a div inside it.\n   */\n  as?: \"div\" | \"article\" | \"section\" | \"main\";\n}\n\nfunction DuckProse({\n  className,\n  measure = \"default\",\n  as: Comp = \"div\",\n  ...props\n}: DuckProseProps) {\n  return (\n    <Comp\n      data-slot=\"duck-prose\"\n      data-measure={measure}\n      className={cn(\n        \"duck-prose max-w-[var(--prose-measure)]\",\n        measures[measure],\n        className\n      )}\n      {...props}\n    />\n  );\n}\n\nexport { DuckProse };\n",
      "type": "registry:ui"
    }
  ],
  "css": {
    "@layer utilities": {
      ":where(.duck-prose)": {
        "font-size": "1.0625rem",
        "line-height": "1.75",
        "color": "var(--foreground)",
        "overflow-wrap": "break-word"
      },
      ":where(.duck-prose) > * + *": {
        "margin-top": "1.25em"
      },
      ":where(.duck-prose) > :first-child": {
        "margin-top": "0"
      },
      ":where(.duck-prose h2, .duck-prose h3, .duck-prose h4)": {
        "font-family": "var(--font-display)",
        "font-weight": "700",
        "line-height": "1.2",
        "letter-spacing": "-0.02em",
        "text-wrap": "balance",
        "scroll-margin-top": "6rem"
      },
      ":where(.duck-prose) > :is(h2, h3, h4) + *": {
        "margin-top": "0.75em"
      },
      ":where(.duck-prose h2)": {
        "font-size": "1.75rem",
        "margin-top": "2.25em"
      },
      ":where(.duck-prose h3)": {
        "font-size": "1.3125rem",
        "margin-top": "1.75em"
      },
      ":where(.duck-prose h4)": {
        "font-size": "1.0625rem",
        "margin-top": "1.5em"
      },
      ":where(.duck-prose a)": {
        "color": "var(--primary)",
        "text-decoration-line": "underline",
        "text-decoration-thickness": "1px",
        "text-underline-offset": "0.2em",
        "text-decoration-color": "color-mix(in oklab, var(--primary) 45%, transparent)",
        "transition": "text-decoration-color 0.2s var(--ease-duck)"
      },
      ":where(.duck-prose a:hover)": {
        "text-decoration-color": "var(--primary)"
      },
      ":where(.duck-prose strong)": {
        "font-weight": "650",
        "color": "var(--foreground)"
      },
      ":where(.duck-prose mark)": {
        "background": "color-mix(in oklab, var(--primary) 22%, transparent)",
        "color": "inherit"
      },
      ":where(.duck-prose small)": {
        "font-size": "0.875em",
        "color": "var(--muted-foreground)"
      },
      ":where(.duck-prose blockquote)": {
        "border-left": "var(--sticker-border) solid var(--primary)",
        "padding-left": "1.25em",
        "color": "var(--muted-foreground)"
      },
      ":where(.duck-prose blockquote cite)": {
        "display": "block",
        "margin-top": "0.75em",
        "font-family": "var(--font-mono)",
        "font-size": "0.75rem",
        "font-style": "normal",
        "letter-spacing": "0.14em",
        "text-transform": "uppercase"
      },
      ":where(.duck-prose ul, .duck-prose ol)": {
        "padding-left": "1.5em"
      },
      ":where(.duck-prose ul)": {
        "list-style-type": "disc"
      },
      ":where(.duck-prose ol)": {
        "list-style-type": "decimal"
      },
      ":where(.duck-prose li)::marker": {
        "color": "var(--primary)"
      },
      ":where(.duck-prose li + li)": {
        "margin-top": "0.4em"
      },
      ":where(.duck-prose li > ul, .duck-prose li > ol)": {
        "margin-top": "0.4em"
      },
      ":where(.duck-prose code)": {
        "font-family": "var(--font-mono)",
        "font-size": "0.875em",
        "background": "var(--muted)",
        "border-radius": "var(--radius-sm)",
        "padding": "0.15em 0.4em"
      },
      ":where(.duck-prose pre)": {
        "font-family": "var(--font-mono)",
        "font-size": "0.875rem",
        "line-height": "1.6",
        "background": "var(--surface, var(--card))",
        "border": "1px solid var(--border)",
        "border-radius": "var(--radius-lg)",
        "padding": "1rem 1.15rem",
        "overflow-x": "auto"
      },
      ":where(.duck-prose pre code)": {
        "background": "none",
        "padding": "0",
        "border-radius": "0",
        "font-size": "inherit"
      },
      ":where(.duck-prose hr)": {
        "border": "0",
        "border-top": "1px solid var(--border)",
        "margin-top": "2.5em",
        "margin-bottom": "2.5em"
      },
      ":where(.duck-prose img, .duck-prose video)": {
        "max-width": "100%",
        "height": "auto",
        "border-radius": "var(--radius-lg)"
      },
      ":where(.duck-prose figcaption)": {
        "margin-top": "0.75em",
        "font-family": "var(--font-mono)",
        "font-size": "0.6875rem",
        "letter-spacing": "0.14em",
        "text-transform": "uppercase",
        "color": "var(--muted-foreground)"
      },
      ":where(.duck-prose-scroll)": {
        "max-width": "100%",
        "overflow-x": "auto"
      },
      ":where(.duck-prose table)": {
        "width": "100%",
        "border-collapse": "collapse",
        "font-size": "0.9375rem",
        "text-align": "left"
      },
      ":where(.duck-prose th)": {
        "font-family": "var(--font-mono)",
        "font-size": "0.6875rem",
        "font-weight": "400",
        "letter-spacing": "0.16em",
        "text-transform": "uppercase",
        "color": "var(--muted-foreground)",
        "border-bottom": "1px solid var(--border)",
        "padding": "0.6em 0.9em",
        "white-space": "nowrap"
      },
      ":where(.duck-prose td)": {
        "border-bottom": "1px solid var(--border)",
        "padding": "0.7em 0.9em",
        "vertical-align": "top"
      },
      ":where(.duck-prose tbody tr:last-child td)": {
        "border-bottom": "0"
      }
    }
  },
  "type": "registry:ui"
}